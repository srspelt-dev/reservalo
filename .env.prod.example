# Variables para docker-compose.prod.yml (copiá a .env en la raíz)
POSTGRES_USER=reservalo
POSTGRES_PASSWORD=cambia-esta-clave-fuerte
POSTGRES_DB=reservalo

# URL pública de la API (la que usa el navegador). Debe coincidir con el dominio del Caddyfile.
PUBLIC_API_URL=https://api.midominio.com
